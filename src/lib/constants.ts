// src/lib/constants.ts

/**
 * URL ของ Google Apps Script Web App ที่ใช้เป็น API ดึงข้อมูลจาก Google Sheets
 */
export const GAS_WEB_APP_URL = process.env.NEXT_PUBLIC_GAS_WEB_APP_URL || process.env.GAS_WEB_APP_URL || "";
